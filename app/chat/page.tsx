import { Metadata } from "next/types";
import React from "react";

export const metadata: Metadata = {
  title: "chat",
  description: "chat route",
};

const Chat = () => {
  return <div className="bg-blue-500 p-10">page</div>;
};

export default Chat;
